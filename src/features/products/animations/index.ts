/**
 * Product animations module
 * Export all animation variants and utilities
 */

export {
  // Animation variants
  cardVariants,
  imageVariants,
  badgePulseVariants,
  badgeShimmerVariants,
  priceVariants,
  overlayVariants,
  addToCartVariants,
  gridContainerVariants,
  gridItemVariants,
  iconVariants,
  
  // Configurations
  SPRING_CONFIG,
  gridStaggerConfig,
  rippleTransition,
  
  // Utilities
  getStaggerDelay,
  calculateMagneticOffset,
} from './productCard.animations';
