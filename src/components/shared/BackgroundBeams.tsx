import { BubbleBackground } from '../ui/shadcn-io/bubble-background';

// Re-export the shadcn bubble background as BackgroundBeams for backward compatibility
export const BackgroundBeams = BubbleBackground;

export default BackgroundBeams;
