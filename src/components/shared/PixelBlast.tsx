import React from 'react';

/**
 * Legacy PixelBlast stub. The project now uses the Shadcn bubble background.
 * This stub returns null to avoid importing heavy WebGL libraries if someone
 * accidentally imports this file. Remove this file entirely if you want to
 * delete the legacy implementation from the repo.
 */
const PixelBlast: React.FC = () => null;

export default PixelBlast;
