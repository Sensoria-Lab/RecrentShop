# Development environment speedups for CRA
# Prevent opening a browser window on start
BROWSER=none
# Disable CRA's ESLint plugin during dev start to reduce initial compilation time
DISABLE_ESLINT_PLUGIN=true
# Optionally disable React Fast Refresh if it causes long rebuilds on your machine
FAST_REFRESH=false
